# comicomi web

Next.js（App Router・静的書き出し）。ルートの `README.md` と `CLAUDE.md` を参照。

- `npm run dev:fixtures` — サンプルデータで起動
- `npm run build:fixtures` — サンプルデータで `out/` を生成
- `npm run lint && npm run typecheck && npm test`
